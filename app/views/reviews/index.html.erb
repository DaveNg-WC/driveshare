<p><strong>Reviews</strong></p>
<ul class="list-group">
  <% @reviews.each do |review| %>
    <li class="list-group-item">
    <%= review.rating %> <%= review.description %>
    <%# <% if review.user == current_user %> %>
      <%= link_to "Delete", review_path(review), method: :delete, data: { confirm: "Are you sure you want to delete this review?" }, class: "btn btn-danger" %>
    <%# <% end %> %>
    </li>
  <% end %>
</ul>
